This needs to be removed, and instead use the default oauth server endpoint to handle proxy requests, by using scopes.
This needs to be removed, and instead use the default oauth server endpoint to handle proxy requests, by using scopes.
